<%= simple_form_for @attendant, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :sector %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                attendants_path, :class => 'btn' %>
  </div>
<% end %>
