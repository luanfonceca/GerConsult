<%= simple_form_for @medical_scale, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :crm %>
  <%= f.input :date %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                medical_scales_path, :class => 'btn' %>
  </div>
<% end %>
