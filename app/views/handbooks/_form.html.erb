<%= simple_form_for @handbook, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :crm %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                handbooks_path, :class => 'btn' %>
  </div>
<% end %>
